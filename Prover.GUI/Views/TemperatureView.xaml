<UserControl x:Class="Prover.GUI.Views.TemperatureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:local="clr-namespace:Prover.GUI.Views.Temperature"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="TestTemplate">
            <ContentControl cal:View.Model="{Binding .}"></ContentControl>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" x:Name="TemperatureHeader" cal:View.Model="{Binding}" cal:View.Context="TemperatureHeader"/>

        <ItemsControl Grid.Row="1" 
                                  ItemTemplate="{StaticResource ResourceKey=TestTemplate}"
                                  ItemsSource="{Binding TestViews}"/>

    </Grid>
</UserControl>
