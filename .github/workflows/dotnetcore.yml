name: .NET Core

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
env:
  solution_path: source\Evc-Prover.sln
  
jobs:
 
  build:

    runs-on: windows-latest    
                  
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
    - name: Current Directory
      run: echo ${solution_path}
    - name: Install dependencies
      run: dotnet restore --verbosity d source\Evc-Prover.sln
    - name: Build
      run: dotnet build --configuration Release --no-restore source\Evc-Prover.sln
    - name: Test
      run: dotnet test --no-restore --verbosity normal source\Evc-Prover.sln
     
