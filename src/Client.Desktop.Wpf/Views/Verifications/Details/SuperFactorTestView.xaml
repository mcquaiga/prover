<reactiveUi:ReactiveUserControl
    x:Class="Client.Wpf.Views.Verifications.Details.SuperFactorTestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Client.Wpf.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Client.Wpf.Views.Verifications.Details"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveUi="http://reactiveui.net"
    xmlns:viewModels="clr-namespace:Application.ViewModels;assembly=Application"
    xmlns:corrections="clr-namespace:Application.ViewModels.Corrections;assembly=Application"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="corrections:SuperFactorViewModel"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Style="{StaticResource SubSectionDividerBorder}">
            <TextBlock
                Grid.Row="0"
                Style="{StaticResource SecondaryTestHeaderStyle}"
                Text="Super. Factor" />
        </Border>
        <controls:PercentageControlNew
            x:Name="PercentError"
            Grid.Row="1"
            Margin="16,0"
            HorizontalAlignment="Center" />

        <UniformGrid
            Grid.Row="2"
            Margin="4"
            Columns="2">
            <controls:ValueDescriptionControl x:Name="ExpectedFactorControl" Label="True Factor" />
            <controls:ValueDescriptionControl x:Name="ActualFactorControl" Label="EVC Factor" />
        </UniformGrid>

    </Grid>
</reactiveUi:ReactiveUserControl>
