<reactiveUi:ReactiveUserControl
    x:Class="Client.Desktop.Wpf.Views.Verifications.TestManagerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveUi="http://reactiveui.net"
    xmlns:verifications="clr-namespace:Client.Desktop.Wpf.ViewModels.Verifications"
    x:Name="TestDetailsUserControl"
    d:DataContext="{d:DesignInstance verifications:TestManagerViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="verifications:TestManagerViewModel"
    mc:Ignorable="d">
    <UserControl.Resources />
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <materialDesign:ColorZone
            Grid.Row="0"
            Padding="16,2"
            DockPanel.Dock="Top"
            Mode="PrimaryLight">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}" Text="QA Test Run" />

                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">

                    <Button
                        x:Name="SaveButton"
                        Margin="8,0"
                        HorizontalAlignment="Left"
                        Content="{materialDesign:PackIcon ContentSave}"
                        Style="{StaticResource MaterialDesignRaisedAccentButton}" />

                    <!--<Button
                        Width="150"
                        Margin="8,2"
                        HorizontalAlignment="Right"
                        Command="{Binding NextTestCommand}"
                        Content="Next Test"
                        Visibility="{Binding ShowNextTestButton, Converter={StaticResource BoolToHiddenConverter}}" />-->
                    <Button
                        Margin="8,0"
                        HorizontalAlignment="Right"
                        Content="{materialDesign:PackIcon Printer}"
                        Style="{StaticResource MaterialDesignRaisedAccentButton}" />
                </StackPanel>

            </Grid>
        </materialDesign:ColorZone>

        <reactiveUi:ViewModelViewHost x:Name="TestViewContent" Grid.Row="1" />

    </Grid>
</reactiveUi:ReactiveUserControl>
