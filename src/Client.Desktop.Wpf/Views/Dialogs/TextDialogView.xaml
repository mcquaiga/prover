<screens:ReactiveDialog
    x:Class="Client.Wpf.Views.Dialogs.TextDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:screens="clr-namespace:Client.Wpf.Screens"
    xmlns:dialogs="clr-namespace:Client.Desktop.Wpf.ViewModels.Dialogs"
    x:TypeArguments="dialogs:TextDialogViewModel"
    mc:Ignorable="d">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel
            Margin="10,10,10,5"
            VerticalAlignment="Center"
            Orientation="Vertical">
            <TextBlock
                x:Name="TitleText"
                Foreground="Black"
                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                Text="Title" />
            <TextBlock
                x:Name="MessageText"
                Foreground="Black"
                Style="{StaticResource MaterialDesignBody2TextBlock}"
                Text="Here's a message." />

            <Button
                x:Name="OkButton"
                Margin="0,15,0,5"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Content="OK"
                Style="{StaticResource MaterialDesignFlatAccentButton}" />
        </StackPanel>
    </Grid>
</screens:ReactiveDialog>
