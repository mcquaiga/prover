<reactiveUi:ReactiveUserControl
    x:Class="Client.Wpf.Views.Verifications.Details.CorrectionTestPointView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveUi="http://reactiveui.net"
    xmlns:viewModels="clr-namespace:Application.ViewModels;assembly=Application"
    xmlns:vm="clr-namespace:Client.Wpf.ViewModels.Verifications"
    d:DataContext="{d:DesignInstance vm:CorrectionTestPointsViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="viewModels:VerificationTestPointViewModel"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <TextBlock
                x:Name="TestLevelBlock"
                Margin="0,0,0,5"
                Style="{StaticResource PrimaryTestHeaderStyle}" />

            <!--<Button
                x:Name="DownloadButton"
                Margin="8,4"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Content="Download"
                Style="{StaticResource MaterialDesignRaisedDarkButton}" />-->
        </Grid>

        <StackPanel
            Grid.Row="1"
            Margin="4"
            Orientation="Vertical">
            <reactiveUi:ViewModelViewHost x:Name="TemperatureContent" />
            <reactiveUi:ViewModelViewHost x:Name="PressureContent" />
            <reactiveUi:ViewModelViewHost x:Name="SuperFactorContent" />
        </StackPanel>

    </Grid>


</reactiveUi:ReactiveUserControl>