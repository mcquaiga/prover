<UserControl x:Class="Prover.GUI.Modules.Certificates.Common.VerificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             TextElement.Foreground="Black"
             TextElement.FontWeight="Light"
             TextElement.FontSize="18"
             FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
             mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="100">
    <UserControl.Resources>
        <ResourceDictionary>

            <BooleanToVisibilityConverter x:Key="VisibilityOfBool" />

            <DataTemplate x:Key="TestTemplate">
                <ContentControl cal:View.Model="{Binding}" cal:View.Context="Grid" />
            </DataTemplate>

            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="Black" />
                <Setter Property="FontSize" Value="18" />
            </Style>

            <Style x:Key="PassFailIcon" TargetType="{x:Type materialDesign:PackIcon}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Instrument.HasPassed}" Value="True">
                        <Setter Property="Kind" Value="Check" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Instrument.HasPassed}" Value="False">
                        <Setter Property="Kind" Value="AlertCircleOutline" />
                        <Setter Property="Foreground" Value="DarkRed"></Setter>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel>
        <Border>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <materialDesign:PackIcon x:Name="Icon"
                   Grid.Column="0"
                   Style="{StaticResource PassFailIcon}"
                   Margin="15"
                   Width="36" Height="36" />
            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="5">
                <TextBlock FontSize="14" Text="{Binding DateTimePretty}" />
                <TextBlock FontSize="24" Text="{Binding Instrument.InstrumentType.Name}" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Instrument.CompositionType}" />
                    <TextBlock Text=" | " />
                    <TextBlock Text="{Binding Instrument.VolumeTest.DriveTypeDiscriminator}" />
                </StackPanel>

            </StackPanel>
            <StackPanel Grid.Column="2" Margin="10 5" Orientation="Vertical" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock>C/N: </TextBlock><TextBlock FontSize="18" Text="{Binding Instrument.SiteNumber2}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>S/N:</TextBlock>
                    <TextBlock FontSize="18" Text="{Binding Instrument.SerialNumber}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>Job #</TextBlock>
                    <TextBlock Text="{Binding Instrument.JobId}" />
                </StackPanel>
            </StackPanel>

            <ContentControl cal:View.Context="TestView"
                            Grid.Column="4"
                            cal:View.Model="{Binding}"/>

            <!--<ContentControl cal:View.Context="VolumeView"
                            Grid.Column="6"
                            cal:View.Model="{Binding}" />-->

        </Grid>
        </Border>
    </DockPanel>

</UserControl>