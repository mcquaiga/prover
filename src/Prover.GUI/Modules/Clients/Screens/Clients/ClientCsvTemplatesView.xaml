<UserControl x:Class="Prover.GUI.Modules.Clients.Screens.Clients.ClientCsvTemplatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Prover.GUI.Modules.Clients.Screens.Clients"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <UniformGrid Rows="1" >
            <GroupBox Header="Instrument Type"
                      BorderThickness="1">
                <ComboBox x:Name="InstrumentTypes" DisplayMemberPath="Name" />
            </GroupBox>
            <GroupBox Header="Verification Type"
                      BorderThickness="1">
                <ComboBox x:Name="VerificationTypes"/>
            </GroupBox>
        </UniformGrid>
        
        <StackPanel Grid.Row="1"
                    Margin="5 15">

            <ItemsControl ItemsSource="{Binding Path=FieldList}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!--<TextBlock Margin="1">
                            [<TextBlock Text="{Binding }"
                                        FontSize="14" />]
                        </TextBlock>-->
                        <Button Content="{Binding }"
                                Command="{Binding AddFieldToTemplateCommand}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <StackPanel Orientation="Vertical">
                <TextBlock Text="CSV Template"/>
                <TextBox x:Name="CsvTemplate"
                         TextWrapping="Wrap"
                         VerticalAlignment="Stretch"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    Margin="8"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"/>
            <Button Content="OK"
                    Command="{Binding OkCommand}"
                    HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</UserControl>
