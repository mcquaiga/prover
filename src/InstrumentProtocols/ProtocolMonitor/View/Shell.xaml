<Window x:Class="ProtocolMonitor.View.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:ProtocolMonitor.View"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Title="{Binding WindowTitle}"
        Height="1000"
        Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <view:SerialPortSettingsView Grid.Row="0" Margin="5" />

            <view:CommandsView Grid.Row="1" Margin="5" IsEnabled="{Binding IsConnected}" />

        </Grid>
        <Grid Grid.Row="1" Width="Auto">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <view:CommWindowView Grid.Column="0" Grid.ColumnSpan="2" Margin="5" />

        </Grid>
    </Grid>
</Window>