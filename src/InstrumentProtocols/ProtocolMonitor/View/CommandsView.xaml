<UserControl x:Class="Prover.ProtocolMonitor.View.CommandsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="clr-namespace:MaterialDesignThemes;assembly=MaterialDesignThemes.Wpf"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="8"/>
        </Style>
    </UserControl.Resources>
    <Grid>
       <materialDesign:Card>
           <UniformGrid>
                <Button Command="{Binding DownloadCommand}" CommandParameter="All">Download Item File</Button>
                <Button Command="{Binding DownloadCommand}" CommandParameter="Temperature">Temperate Items</Button>
                <Button Command="{Binding DownloadCommand}" CommandParameter="Pressure">Pressure Items</Button>
                <Button Command="{Binding DownloadCommand}" CommandParameter="Volume">Volume Items</Button>
            </UniformGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>