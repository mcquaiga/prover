using System;
using System.Collections.Generic;
using System.Linq.Expressions;
using System.Threading.Tasks;
using Domain.EvcVerifications;
using Shared.Interfaces;

namespace Domain.Repositories
{
    //public interface IEvcVerificationRepository : IAsyncRepositoryGuid<EvcVerificationTest>
    //{
    //    Task<T> AddAsync<T>(EvcVerificationTest entity);
    //    Task<int> CountAsync(Expression<Func<EvcVerificationTest, bool>> predicate);
    //    Task DeleteAsync(EvcVerificationTest entity);
    //    Task DeleteAsync(Guid id);
    //    Task<EvcVerificationTest> GetAsync(Guid id);
    //    Task<IReadOnlyList<EvcVerificationTest>> ListAsync(Expression<Func<EvcVerificationTest, bool>> predicate);
    //    Task UpdateAsync(EvcVerificationTest entity);
    //}
}