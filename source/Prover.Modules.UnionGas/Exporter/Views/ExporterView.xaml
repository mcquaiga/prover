<reactiveUi:ReactiveUserControl
    x:Class="Prover.Modules.UnionGas.Exporter.Views.ExporterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Prover.Modules.UnionGas.Exporter.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveUi="http://reactiveui.net"
    x:Name="ExporterViewControl"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="local:ExporterViewModel"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Client.Desktop.Wpf;component/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>

        <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">
            <materialDesign:DrawerHost.LeftDrawerContent>
                <DockPanel MinWidth="300" VerticalAlignment="Stretch">
                    <ToggleButton
                        Margin="8"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Top"
                        IsChecked="{Binding ElementName=MenuToggleButton, Path=IsChecked, Mode=TwoWay}"
                        Style="{StaticResource MaterialDesignHamburgerToggleButton}" />

                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="8">
                            <TextBlock Text="Export Tests by Job #" />
                            <ComboBox
                                x:Name="JobIdsComboBox"
                                Width="Auto"
                                DisplayMemberPath="."
                                Style="{StaticResource MaterialDesignComboBox}" />
                            <Button
                                x:Name="ExportTestsByJobNumberButton"
                                Margin="10"
                                HorizontalAlignment="Right"
                                CommandParameter="{Binding Path=SelectedItem, ElementName=JobIdsComboBox}"
                                Content="Export"
                                Style="{StaticResource MaterialDesignFlatAccentButton}" />
                        </StackPanel>

                        <StackPanel
                            Grid.Row="1"
                            Margin="8"
                            Orientation="Vertical">
                            <TextBlock Text="Export Failed Test" />
                            <TextBox
                                Width="Auto"
                                materialDesign:HintAssist.Hint="Enter Inventory #"
                                Text="{Binding FailedCompanyNumber}" />
                            <Button
                                x:Name="ExportFailedTestButton"
                                Margin="10"
                                HorizontalAlignment="Right"
                                Command="{Binding ExportFailedTestCommand}"
                                Content="Export"
                                Style="{StaticResource MaterialDesignFlatAccentButton}" />
                        </StackPanel>

                        <reactiveUi:ViewModelViewHost
                            x:Name="TestsByJobNumberContentControl"
                            Grid.Row="2"
                            Margin="8" />


                    </Grid>

                </DockPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <materialDesign:ColorZone
                    Grid.Row="0"
                    Padding="5"
                    materialDesign:ShadowAssist.ShadowDepth="Depth0"
                    Mode="PrimaryLight">
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>


                        <ToggleButton
                            x:Name="MenuToggleButton"
                            Grid.Column="0"
                            IsChecked="False"
                            Style="{StaticResource MaterialDesignHamburgerToggleButton}" />



                        <StackPanel
                            Grid.Column="1"
                            Margin="8,2"
                            Orientation="Vertical">
                            <!--<TextBlock Style="{StaticResource MaterialDesignTextBlock}" Text="Filter by Instrument Type" />-->
                            <StackPanel Margin="0,2" Orientation="Horizontal">
                                <StackPanel.Resources>
                                    <Style BasedOn="{StaticResource MaterialDesignFlatButton}" TargetType="{x:Type Button}">
                                        <Setter Property="Margin" Value="2,0" />
                                        <Setter Property="Width" Value="Auto" />
                                        <Setter Property="Height" Value="Auto" />
                                    </Style>
                                </StackPanel.Resources>
                                <ItemsControl
                                    x:Name="DeviceTypes"
                                    Grid.Row="1"
                                    DataContext="{Binding}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel
                                                Margin="15,0"
                                                VerticalAlignment="Center"
                                                Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button
                                                Margin="4,0"
                                                Command="{Binding ViewModel.FilterByTypeCommand, ElementName=ExporterViewControl}"
                                                CommandParameter="{Binding}"
                                                Content="{Binding Name}"
                                                FontSize="16" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </StackPanel>
                        </StackPanel>
                        <materialDesign:PopupBox PlacementMode="BottomAndAlignRightEdges" StaysOpen="False">
                            <StackPanel>
                                <CheckBox
                                    x:Name="IncludeExportedCheckBox"
                                    Margin="8"
                                    VerticalAlignment="Center"
                                    Content="Include Exported"
                                    FontSize="14"
                                    Style="{StaticResource MaterialDesignDarkCheckBox}" />
                                <CheckBox
                                    x:Name="IncludeArchivedCheckBox"
                                    Margin="8"
                                    VerticalAlignment="Center"
                                    Content="Include Archived"
                                    FontSize="14"
                                    Style="{StaticResource MaterialDesignDarkCheckBox}" />
                            </StackPanel>
                        </materialDesign:PopupBox>
                        <StackPanel
                            Grid.Column="2"
                            Margin="15,0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Orientation="Horizontal">

                            <CheckBox
                                x:Name="IncludeExportedCheckBox"
                                Margin="8"
                                VerticalAlignment="Center"
                                Content="Include Exported"
                                FontSize="14"
                                Style="{StaticResource MaterialDesignDarkCheckBox}" />
                            <CheckBox
                                x:Name="IncludeArchivedCheckBox"
                                Margin="8"
                                VerticalAlignment="Center"
                                Content="Include Archived"
                                FontSize="14"
                                Style="{StaticResource MaterialDesignDarkCheckBox}" />

                        </StackPanel>

                        <TextBlock
                            x:Name="TestCountTextBlock"
                            Grid.Column="3"
                            Margin="40,5"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Style="{StaticResource MaterialDesignBody1TextBlock}" />
                    </Grid>
                </materialDesign:ColorZone>
                <!--<ProgressBar
                        Grid.Row="1"
                        Width="56"
                        Height="56"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Panel.ZIndex="5"
                        IsIndeterminate="True"
                        Style="{StaticResource MaterialDesignCircularProgressBar}"
                        Value="0" />-->

                <ListBox
                    x:Name="VisibleItemsListBox"
                    Grid.Row="1"
                    Margin="5"
                    HorizontalAlignment="Stretch">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!--
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                -->

            </Grid>

        </materialDesign:DrawerHost>

    </Grid>
</reactiveUi:ReactiveUserControl>
