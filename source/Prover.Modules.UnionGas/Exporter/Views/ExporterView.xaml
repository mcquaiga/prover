<reactiveUi:ReactiveUserControl
    x:Class="Prover.Modules.UnionGas.Exporter.Views.ExporterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Prover.Modules.UnionGas.Exporter.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveUi="http://reactiveui.net"
    x:Name="ExporterViewControl"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="local:ExporterViewModel"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Client.Desktop.Wpf;component/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>

        <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">
            <materialDesign:DrawerHost.LeftDrawerContent>
                <DockPanel MinWidth="212">
                    <ToggleButton
                        Margin="8"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Top"
                        IsChecked="{Binding ElementName=MenuToggleButton, Path=IsChecked, Mode=TwoWay}"
                        Style="{StaticResource MaterialDesignHamburgerToggleButton}" />

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel
                            Grid.Row="0"
                            Margin="5"
                            Orientation="Vertical">
                            <TextBlock Text="Export Failed Test" />
                            <TextBox Width="Auto" Text="{Binding FailedCompanyNumber}" />
                            <Button
                                Command="{Binding ExportFailedTestCommand}"
                                Content="Export Failed"
                                Style="{StaticResource MaterialDesignFlatAccentButton}" />
                        </StackPanel>

                        <!--<ContentControl
                        x:Name="TestsByJobNumber"
                        Grid.Row="1"
                        Margin="5"
                        ="{Binding TestsByJobNumber}" />-->
                    </Grid>

                </DockPanel>
            </materialDesign:DrawerHost.LeftDrawerContent>
            <Grid>

                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <materialDesign:ColorZone
                            Padding="12"
                            materialDesign:ShadowAssist.ShadowDepth="Depth0"
                            Mode="PrimaryLight">
                            <DockPanel>
                                <ToggleButton
                                    x:Name="MenuToggleButton"
                                    IsChecked="False"
                                    Style="{StaticResource MaterialDesignHamburgerToggleButton}" />
                            </DockPanel>
                        </materialDesign:ColorZone>

                        <StackPanel
                            Grid.Column="0"
                            Margin="8"
                            Orientation="Vertical">
                            <TextBlock Style="{StaticResource MaterialDesignCaptionTextBlock}" Text="Filter by Instrument Type" />
                            <StackPanel Margin="0,2" Orientation="Horizontal">
                                <StackPanel.Resources>
                                    <Style BasedOn="{StaticResource MaterialDesignFlatButton}" TargetType="{x:Type Button}">
                                        <Setter Property="Margin" Value="2,0" />
                                        <Setter Property="Width" Value="Auto" />
                                        <Setter Property="Height" Value="Auto" />
                                    </Style>
                                </StackPanel.Resources>
                                <ItemsControl
                                    x:Name="DeviceTypes"
                                    Grid.Row="1"
                                    DataContext="{Binding}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button
                                                Command="{Binding Path=ViewModel.FilterByTypeCommand, ElementName=ExporterViewControl}"
                                                CommandParameter="{Binding}"
                                                Content="{Binding Name}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </StackPanel>
                        </StackPanel>

                    </StackPanel>

                    <!--<ProgressBar
                        Grid.Row="1"
                        Width="56"
                        Height="56"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Panel.ZIndex="5"
                        IsIndeterminate="True"
                        Style="{StaticResource MaterialDesignCircularProgressBar}"
                        Value="0" />-->

                    <ListBox
                        x:Name="VisibleItemsListBox"
                        Grid.Row="1"
                        Margin="5"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Id}" />
                                    <TextBlock Text="{Binding TestDateTime}" />
                                    <Button
                                        Command="{Binding ViewModel.PrintReport, ElementName=ExporterViewControl}"
                                        CommandParameter="{Binding}"
                                        Content="{materialDesign:PackIcon PrintPreview}" />

                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <!--<ListBox
                        x:Name="VisibleItemsListBox"
                        Grid.Row="1"
                        Margin="5"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch"
                        Panel.ZIndex="0">
                        <ListBox.ItemContainerStyle>
                            <Style BasedOn="{StaticResource {x:Type ListBoxItem}}" TargetType="{x:Type ListBoxItem}">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                            <Border BorderBrush="Gray" BorderThickness="0,0,0,1">
                                                <ContentPresenter />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>-->


                    <!--<Button Margin="8"
                    Grid.Row="2"
                    HorizontalAlignment="Left"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding ExportAllPassedQaRunsCommand}"
                    Content="Export all passed">
            </Button>-->


                </Grid>
            </Grid>
        </materialDesign:DrawerHost>

    </Grid>
</reactiveUi:ReactiveUserControl>
