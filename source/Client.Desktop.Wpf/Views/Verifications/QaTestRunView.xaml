<UserControl
    x:Class="Client.Desktop.Wpf.Views.Verifications.QaTestRunView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"
    xmlns:reactiveUi="http://reactiveui.net"
    d:DesignHeight="400"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Verifications/Styles.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.Indigo.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Accent/MaterialDesignColor.Amber.xaml" />
                <ResourceDictionary>
                    <Style BasedOn="{StaticResource MaterialDesignFloatingHintComboBox}" TargetType="{x:Type ComboBox}">
                        <Setter Property="FontSize" Value="18" />
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <materialDesign:ColorZone
            Grid.Row="0"
            Mode="PrimaryLight"
            Style="{StaticResource SubheaderToolbarColorZoneStyle}">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Text="QA TEST RUN" />

                <StackPanel
                    x:Name="EditTestToolBarControl"
                    Grid.Column="1"
                    HorizontalAlignment="Left"
                    Orientation="Horizontal">

                    <Button
                        x:Name="SubmitTestButton"
                        Width="Auto"
                        Margin="25,0,5,0"
                        HorizontalAlignment="Right"
                        Content="SUBMIT TEST"
                        Style="{StaticResource MaterialDesignRaisedDarkButton}" />

                    <Button
                        x:Name="SaveButton"
                        HorizontalAlignment="Left"
                        Content="{materialDesign:PackIcon ContentSave}"
                        Style="{StaticResource MaterialDesignIconButton}" />
                    <!--  Command="{Binding ViewModel.PrintTestReport}"  -->
                    <Button
                        x:Name="PrintButton"
                        HorizontalAlignment="Left"
                        Content="{materialDesign:PackIcon Printer}"
                        Style="{StaticResource MaterialDesignIconButton}" />

                </StackPanel>

            </Grid>
        </materialDesign:ColorZone>


    </Grid>
</UserControl>
